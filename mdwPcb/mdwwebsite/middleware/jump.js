/*
 * @Descripttion:
 * @version: 1.0
 * @Author: Teemor
 * @Date: 2021-08-04 16:22:50
 */
export default function ({ isServer, req, redirect, route }) {
    // if (process.browser) {
    // const isMobile = (ua) => {
    //     return !!ua.match(/AppleWebKit.*Mobile*/)
    // }
    // const userAgent = req ? req.headers['user-agent'] : ''
    // if (route.fullPath.includes('/phone') && !isMobile(userAgent)) {
    //     const url = '/'
    //     redirect(url)
    // }
    // if (!route.fullPath.includes('/phone') && isMobile(userAgent)) {
    //     redirect('/phone' + route.fullPath)
    // }
    // }
}
